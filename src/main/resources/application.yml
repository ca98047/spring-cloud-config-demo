server:
  port: 8888

spring:
  application:
    name: "config-server"
  profiles:
    active: devel
  cloud:
    config:
    server:
    git:
    uri: "https://github.com/ca98047/spring-cloud-config-demo"

application:
  message: "Message From Default Local Git Repository (Spring Cloud Bus)"